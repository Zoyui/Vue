<!--
    Vue数据语法有两种：    
      1. 单向绑定（v-bind 或者简写成 :）： 数据只能从data流向页面
      2. 双向绑定（v-model）：数据既从data流向页面，又从页面流向data
            备注：
                1. 双向绑定一般都应用在表单类元素上（如：input、select等）
                2. v-model：value 可以简写为v-model，因为v-model默认收集的就是value值
-->
