<!--
    el与data有两种写法：    
      1. el的两种写法
        （1）new Vue时候配置el属性
        （2）先创建Vue实例，随后再通过v.$mount('#root')指定el的值
      2. data的两种写法
        （1）对象式
        （2）函数式
        如何选择：目前哪种写法都可以，以后在学习组件时，data必须使用函数式，否则会报错
      3. 一个重要的原则：
        由Vue管理的函数，一定不要写箭头函数，一旦写了箭头函数，this就不再是Vue实例了
            data:()=>{
            console.log('@@@', this) //此处的this是window实例对象,因为箭头函数没有自己的this
            return{
                name:'Zoyui'
              }
            }
-->
